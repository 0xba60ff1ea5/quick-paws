# Common links
URL_HOME = "https://www.furaffinity.net/"
def URL_USER(uname):
	return URL_HOME + "user/" + uname + "/"
def URL_GALLERY(uname):
	return URL_HOME + "gallery/" + uname + "/"
def URL_SCRAPS(uname):
	return URL_HOME + "scraps/" + uname + "/"
def URL_FAVORITES(uname):
	return URL_HOME + "favorites/" + uname + "/"
def URL_JOURNALS(uname):
	return URL_HOME + "journals/" + uname + "/"
def URL_COMMISSIONS(uname):
	return URL_HOME + "commissions/" + uname + "/"
def URL_STATS(uname):
	return URL_HOME + "stats/" + uname + "/submissions/"
